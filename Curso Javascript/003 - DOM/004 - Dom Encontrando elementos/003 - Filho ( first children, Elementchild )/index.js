// Pegando elementos do container

const container = document.querySelector(".container");

console.log(container.firstChild);// pega o primeiro elemento filho que é text um pedaço de espaço
console.log(container.firstElementChild);// pega o primeiro elemento filho que é texto do html " Encontrando Elemento", agora sim



//Pegando elemento do paragrafo

const p = document.querySelector(".paragrafo");

console.log(p.firstChild);//Pega o texto em loren if inteiro
console.log(p.firstElementChild);//Pegou o Strong